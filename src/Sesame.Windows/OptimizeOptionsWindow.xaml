<Window x:Class="Sesame.OptimizeOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Optimize"
        Icon="pack://application:,,,/Assets/sesame-taskbar.ico"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Bg}"
        Foreground="{DynamicResource Text}">
    <StackPanel Margin="20" Width="520">
        <TextBlock FontWeight="SemiBold" FontSize="15" Text="Options before it starts"/>
        <TextBlock x:Name="CountText" Foreground="{DynamicResource Muted}" Margin="0,6,0,14" TextWrapping="Wrap"/>
        <CheckBox x:Name="OverwriteBox" Content="Update existing shortcuts" Margin="0,0,0,8"/>
        <CheckBox x:Name="ArtworkBox" Content="Overwrite artwork" Margin="0,0,0,8"
                  ToolTip="Replace existing Steam covers. Unchanged files are skipped."/>
        <CheckBox x:Name="MaskBox" Content="Use category masks"
                  ToolTip="Master switch. Per platform (Hydra off by default) you set masks in Settings → Covers."/>
        <TextBlock x:Name="GyroNote" TextWrapping="Wrap" Foreground="{DynamicResource Muted}" Margin="0,12,0,0"
                   Visibility="Collapsed"/>
        <TextBlock x:Name="SteamNote" TextWrapping="Wrap" Foreground="{DynamicResource Muted}" Margin="0,16,0,0"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel" IsCancel="True" Click="Cancel_Click"/>
            <Button Content="Start" Style="{StaticResource AccentButton}" IsDefault="True"
                    Click="Start_Click" Margin="0"/>
        </StackPanel>
    </StackPanel>
</Window>
